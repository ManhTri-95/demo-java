spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/demo
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update      # dev thôi — sau này thay bằng Flyway
    show-sql: true
    properties:
      hibernate:
        format_sql: true